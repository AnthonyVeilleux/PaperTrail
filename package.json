{
  "dependencies": {
    "git": "^0.1.5"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "http-server": "^14.1.1",
    "jest": "^30.2.0"
  },
  "jest": {
    "testPathIgnorePatterns": ["tests/e2e"]
  },
  "scripts": {
    "test": "jest",
    "test:e2e": "node tests/build-html.js && playwright test",
    "test:all": "npm test && npm run test:e2e"
  }
}
